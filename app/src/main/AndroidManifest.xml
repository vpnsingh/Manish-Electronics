<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shubham.abhi.manish_electronics">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Location_activity"
            android:label="Select Location" />
        <activity
            android:name=".Order"
            android:label="Select your Order" />

        <activity
            android:name=".Paymentpage"
            android:label="Confirm your Payment" />
        <activity android:name=".Ordercancelled"
            android:label="Order Cancelled"/>
        <activity
            android:name=".Confirmation_page"
            android:label="Confirmation Page" />
        <!-- <activity android:name=".OrderLocationService"></activity> -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Logout"
            android:label="@string/Logout"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>